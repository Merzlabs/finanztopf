import { FilterPipe } from './filter-translate.pipe';

describe('FilterTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipe();
    expect(pipe).toBeTruthy();
  });
});
